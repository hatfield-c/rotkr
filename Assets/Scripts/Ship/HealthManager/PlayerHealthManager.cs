using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerHealthManager : AbstractHealthManager
{
    // Start is called before the first frame update
    new void Start()
    {
        base.Start();
    }

    void Update(){
        
    }
}
